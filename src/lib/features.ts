/**
 * Feature flags.
 *
 * TEMP: set SKIP_ADMIN_UNLOCK to false to restore the host unlock gate.
 * Do not delete the unlock flow — only skip it while this is true.
 */
export const SKIP_ADMIN_UNLOCK = true;
